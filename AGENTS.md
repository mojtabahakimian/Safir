# راهنمای عامل‌های هوش مصنوعی — Safir

این فایل برای هر عاملی است که روی این مخزن کار می‌کند (Jules، Codex،
Claude Code، …). قبل از تغییر دادن کد بخوانیدش — چند تله‌ی غیرواضح اینجا هست
که بدون دانستنشان وقت زیادی هدر می‌رود.

---

## ۱. ساختار پروژه

| پروژه | نوع | توضیح |
|---|---|---|
| `Client` | Blazor WebAssembly | رابط کاربری، MudBlazor 6.21 |
| `Server` | ASP.NET Core | API، Dapper، گزارش‌ها |
| `Shared` | Class Library | مدل‌ها و ابزارهای مشترک |

.NET 8 · `ImplicitUsings` روشن · بانک اطلاعاتی SQL Server

```bash
dotnet build Safir.sln -c Debug             # باید ۰ خطا بدهد (هشدار زیاد است، طبیعی)
dotnet test  Safir.sln                      # تست‌های واحد — به دیتابیس نیاز ندارند
python3 .github/scripts/check_pay2_acl.py   # نگهبان کنترل دسترسی
```

تست سرتاسری در مرورگر: `Tests/e2e/README.md` را ببینید.

---

## ۲. تله‌های مهم — این‌ها را حتماً بدانید

### `Server/Info/ScriptSqly.cs` کامپایل نمی‌شود

در `Server/Safir.Server.csproj` این خط هست:

```xml
<Compile Remove="Info\ScriptSqly.cs" />
```

این فایل **عمداً** از کامپایل خارج شده. یک نرم‌افزار جداگانه آن را اجرا
می‌کند تا دیتابیس مشتری را با اسکریپت به‌روز کند. پس:

* خرابی نحوی در این فایل **باعث شکست build این solution نمی‌شود** — قبل از
  اینکه چیزی را «باگ کامپایل» اعلام کنید، `dotnet build` بگیرید.
* هر مهاجرت دیتابیس باید **هم** در `ScriptSqly.cs` باشد **و هم** به‌صورت یک
  فایل `.sql` مستقل در `Server/Database/` تا بشود جدا اجرایش کرد.
* داخل این فایل، اسکریپت‌ها رشته‌ی verbatim (`@"..."`) هستند و روی `GO`
  تکه‌تکه می‌شوند؛ پس `"` باید `""` نوشته شود.

### `Server/Info/` مستندات است، نه کد زنده

`PAY2_Procedures_v6.sql` و امثالش عکس لحظه‌ای‌اند و می‌توانند از موتور زنده
عقب باشند. **منبع حقیقت، `ScriptSqly.cs` است.** نمونه‌ی واقعی این اختلاف:

| | `PAY2_Procedures_v6.sql` (قدیمی) | `ScriptSqly.cs` (زنده) |
|---|---|---|
| `GROSS_PAY` | ریل اسمی (`BASE_SAL`) | ریل **رسمی** (`BASE_SAL_B`) |
| `NOMINAL_GROSS` | وجود ندارد | ریل اسمی (`BASE_SAL`) |

اگر عددی جور درنیامد، اول این را چک کنید.

### `schema.sql` جدول‌های ورود را ندارد

`SALA_DTL` (کاربران)، `SAL_CHEK` (دسترسی فرم‌ها) و `CHARTSAZMANI` در
`schema.sql` و `legacy_dependencies.sql` **نیستند**. دیتابیسی که فقط از
آن‌ها ساخته شود هیچ کاربری ندارد و نمی‌شود واردش شد.
`Server/Database/test_auth_and_acl_users.sql` این سه جدول را می‌سازد و سه
کاربر آزمایشی درج می‌کند.

### برچسب‌های فرم به ورودی‌هایشان وصل نیستند

MudBlazor 6.21.0 برچسب را با `for="mudinput-{guid}"` می‌سازد ولی هیچ `id` ای
روی خود `<input>` نمی‌گذارد. نتیجه: هیچ‌کدام از `label[for]`های برنامه به
عنصری اشاره نمی‌کنند که وجود داشته باشد (روی صفحه ورود: ۴ برچسب، ۰ ورودی
دارای id).

اثرش دو چیز است:

* **دسترس‌پذیری** — صفحه‌خوان نمی‌تواند بگوید هر فیلد چیست، و کلیک روی برچسب
  فوکوس را به ورودی نمی‌برد. این روی **همه‌ی فرم‌های برنامه** اثر دارد.
* **تست** — `page.getByLabel('نام کاربری')` کار نمی‌کند. از کمکی
  `field(page, 'برچسب')` در `Tests/e2e/helpers/app.js` استفاده کنید.

رفعش یا ارتقای MudBlazor است یا دادن `id` دستی به هر فیلد؛ هر دو دامنه‌ی
گسترده‌ای دارند، پس بدون هماهنگی انجامش ندهید.

### رمز عبور در دیتابیس کدشده است، نه هش

`Shared/Utility/CL_METHODS.cs` → `DECODEUN` (نام کاربری، +۲۰ روی بایت‌های
CP1256) و `DECODEPS` (رمز، +۱۰ و بعد حذف ۳ کاراکتر از هر طرف).
برای ساختن کاربر جدید باید مقدار کدشده را حساب کنید. مقادیر موجود در
`test_auth_and_acl_users.sql` با اجرای واقعی همین توابع راستی‌آزمایی شده‌اند.

---

## ۳. کنترل دسترسی حقوق و دستمزد (PAY2)

دو لایه دارد:

1. **دسترسی فرم** — `TFORMS` (تعریف فرم) + `SAL_CHEK` (مجوز هر کاربر).
   پنج مجوز: `RUN=1`، `SEE=2`، `INP=4`، `UPD=8`، `DEL=16`.
2. **محدوده کارگاه** — `PAY2_USER_WS` مشخص می‌کند هر کاربر کدام کارگاه‌ها
   را می‌بیند. لیست‌ها هم بر همین اساس فیلتر می‌شوند، نه فقط دکمه‌ها.

دو کلید در `PAY2_CONFIG`:

* `ACL_ENFORCE` — پیش‌فرض `'0'` (خاموش) تا نصب روی مشتری چیزی را قطع نکند.
* `ACL_WS_SCOPE_ENFORCE` — پیش‌فرض `'1'`.

### قانون: هر اکشن جدید باید مجوز داشته باشد

```csharp
[HttpPost("save")]
[Pay2Authorize(Pay2Forms.Employee, Pay2Perm.Inp)]
public async Task<IActionResult> Save(...) { ... }
```

`.github/scripts/check_pay2_acl.py` در CI اجرا می‌شود و اگر اکشنی در
`Server/Controllers/Pay2*.cs` بدون `[Pay2Authorize]` (یا `HasAsync` در بدنه)
بماند، یا `[AllowAnonymous]` بگیرد، build را قرمز می‌کند. استثناها باید در
`ALLOWLIST` همان فایل با دلیل ثبت شوند.

---

## ۴. محیط تست کامل

```bash
export ConnectionStrings__DefaultConnection='Server=localhost,1433;Database=SafirTestDb;User Id=sa;Password=...;TrustServerCertificate=True'
export Jwt__Key='...'
./scripts/setup-test-env.sh
```

این اسکریپت SQL Server را نصب می‌کند، دیتابیس `SafirTest*` را از نو
می‌سازد، مهاجرت ACL و داده نمونه را می‌ریزد، build می‌گیرد و یک آزمون دود
**واقعی** انجام می‌دهد. گاردهای ایمنی: فقط `localhost` و فقط نامی که با
`SafirTest` شروع شود (چون دیتابیس را `DROP` می‌کند).

کاربران آزمایشی:

| نام کاربری | رمز | نقش |
|---|---|---|
| `payadmin` | `111111` | دسترسی کامل، همه کارگاه‌ها |
| `payviewer` | `222222` | فقط `RUN`+`SEE` |
| `payscoped` | `333333` | دسترسی کامل، فقط کارگاه ۱ |

### آنچه بدون دیتابیس هم قابل تست است

برنامه بدون دیتابیس بالا می‌آید — Dapper فقط موقع اجرای کوئری وصل می‌شود.
پس این‌ها همیشه قابل اجرا هستند و در CI هم اجرا می‌شوند:

* `dotnet test` — ۳۳ تست واحد روی فیلتر `[Pay2Authorize]` و ریاضی بیتی مجوزها
* `Tests/e2e/tests/01-smoke.spec.js` — ۱۲ تست در مرورگر واقعی

تست‌هایی که به دیتابیس نیاز دارند (`02-acl.spec.js`) خودشان skip می‌شوند،
نه fail. شکستِ محیط را با شکستِ برنامه قاطی نکنید.

### آزمون دود سطحی ننویسید

`/` در Blazor WebAssembly یک فایل استاتیک است و **حتی وقتی دیتابیس و کل API
خراب باشد هم ۲۰۰ برمی‌گرداند**. اگر فقط `/` را چک کنید، هیچ چیز را تست
نکرده‌اید. سه چیز را جدا بررسی کنید:

1. `GET /_framework/blazor.boot.json` → کلاینت واقعاً بالا می‌آید
2. `POST /api/auth/login` → دیتابیس، Dapper، JWT و کدگشایی رمز سالم‌اند
3. `GET /api/pay2/access/me` بدون توکن ۴۰۱ و با توکن ۲۰۰ → ACL فعال است

### تست انسانی با مرورگر

بعد از بالا آمدن برنامه روی `http://127.0.0.1:5080`، با Playwright وارد شوید
و همان مسیرهایی را بروید که کاربر می‌رود. ارزشمندترین سناریو مقایسه‌ی سه
کاربر بالاست: `payviewer` نباید دکمه‌ی ذخیره ببیند و اگر مستقیم API را صدا
بزند باید ۴۰۳ بگیرد؛ `payscoped` نباید کارگاه‌های دیگر را در لیست ببیند.

---

## ۵. راستی‌آزمایی محاسبات حقوق

```bash
# در SSMS: Server/Tools/pay2_export.sql را اجرا و نتیجه را ذخیره کنید
python3 Server/Tools/pay2_audit.py pay2_export.json
```

معادله‌های موتور محاسبه را روی داده‌ی واقعی بررسی می‌کند بدون اینکه به
دیتابیس وصل شود. جزئیات در `Server/Tools/README.md`.

نکته‌ای که این ابزار پیدا کرده و هنوز در موتور رفع نشده: در
`PAY2_ATTENDANCE` دو ستون `DAYS` (روز رسمی) و `DAYSB` (روز اسمی) هستند و
هیچ اعتبارسنجی‌ای بین‌شان نیست. اگر یکی صفر و دیگری غیرصفر باشد فیش خراب
می‌شود بدون هیچ خطایی. ستون `WORK_DAYS` مبنای محاسبه **نیست**.

---

## ۶. نکات امنیتی شناخته‌شده

* `Server/Controllers/AuthController.cs` — یک رمز عبور دور زننده
  (`bypassLogin`) در کد هست که روی همه‌ی کاربرها کار می‌کند. عمدی بوده ولی
  در محیط واقعی یک ریسک جدی است.
* رمزها هش نشده‌اند، فقط کدشده‌اند (بخش ۲ را ببینید).
* خروجی `pay2_export.json` داده‌ی واقعی پرسنل دارد؛ در `.gitignore` هست و
  نباید کامیت شود.

---

## ۷. سبک کار

* پیام‌های خطای رو به کاربر فارسی‌اند — همین را ادامه دهید.
* کامنت‌ها در بخش حقوق و دستمزد فارسی‌اند؛ در بقیه‌ی کد انگلیسی. از سبک
  فایلی که ویرایش می‌کنید تقلید کنید.
* قبل از اعلام «باگ»، `dotnet build` بگیرید. مخصوصاً بخش ۲ را بخوانید.

# راهنمای عامل‌های هوش مصنوعی — Safir

این فایل برای هر عاملی است که روی این مخزن کار می‌کند (Jules، Codex، Claude
Code، یا هر عامل دیگری که این استاندارد را می‌خواند). قبل از تغییر دادن کد
بخوانیدش. هدف این سند سه چیز است:

1. بفهمید این پروژه چیست و چطور کار می‌کند (بخش ۱ و ۲)
2. بدون کمک انسان، یک محیط تست **واقعی** (با SQL Server واقعی، نه شبیه‌سازی)
   برای خودتان راه بیندازید — در هر محیطی که هستید، حتی اگر شبکه‌اش محدود
   باشد (بخش ۴)
3. بدانید «تحویل آماده‌ی Production» برای این پروژه دقیقاً یعنی چه — یک
   چک‌لیست قابل اجرا، نه یک شعار (بخش ۵)

اگر وقت کم دارید و فقط یک بخش را می‌خوانید، بخش ۲ (تله‌های مهم) و بخش ۵
(چک‌لیست تحویل) را بخوانید.

---

## ۱. این پروژه چیست

**Safir** یک نرم‌افزار جامع حسابداری و مدیریت سازمانی است. نکته‌ی مهم برای
یک عامل هوش مصنوعی: **این مخزن تنها مصرف‌کننده‌ی این دیتابیس نیست.**

- یک نرم‌افزار WPF قدیمی و جداگانه (دسکتاپ، حسابداری) به همین دیتابیس وصل
  است و همچنان در حال استفاده‌ی روزانه‌ی مشتری‌هاست.
- این مخزن (Blazor + ASP.NET Core) نسخه‌ی وب/جدیدتر است که دارد به‌مرور
  بخش‌های بیشتری از همان دیتابیس را پوشش می‌دهد.
- **نتیجه‌ی عملی:** نمی‌شود جدول‌ها یا رفتارهای قدیمی را «تمیز» یا «مدرن»
  کرد چون منطقی‌تر به‌نظر می‌رسد — نرم‌افزار WPF همچنان به همان قرارداد
  قدیمی وابسته است. رمزنگاری غیر‌استاندارد رمز عبور (نه هش)، فیلتر
  `ENABL=0` به‌جای `ENABL=1` برای «کاربر فعال»، و امثال آن **عمدی و پابرجا**
  هستند مگر صریحاً خلافش خواسته شود. جزئیات در بخش ۲.

### ماژول PAY2 (حقوق و دستمزد)

جدیدترین و پیچیده‌ترین بخش این مخزن. محاسبه‌ی حقوق، بیمه، مالیات، احکام
کارگزینی، مساعده، وام، تسویه حساب — و از این PR به بعد، یک سیستم کامل
کنترل دسترسی (بخش ۳). اگر کاری به این ماژول دارید، `Client/Pages/Salary/`
و `Server/Controllers/Pay2*.cs` نقطه‌ی شروع‌اند.

---

## ۲. ساختار پروژه و تله‌های مهم

| پروژه | نوع | توضیح |
|---|---|---|
| `Client` | Blazor WebAssembly | رابط کاربری، MudBlazor 6.21 |
| `Server` | ASP.NET Core | API، Dapper، گزارش‌ها |
| `Shared` | Class Library | مدل‌ها و ابزارهای مشترک |

.NET 8 · `ImplicitUsings` روشن · بانک اطلاعاتی SQL Server

```bash
dotnet build Safir.sln -c Debug             # باید ۰ خطا بدهد (هشدار زیاد است، طبیعی)
dotnet test  Safir.sln                      # تست‌های واحد — به دیتابیس نیاز ندارند
python3 .github/scripts/check_pay2_acl.py   # نگهبان کنترل دسترسی PAY2
```

تست سرتاسری در مرورگر: `Tests/e2e/README.md`.

### `Server/Info/ScriptSqly.cs` کامپایل نمی‌شود — عمدی است

در `Server/Safir.Server.csproj`:

```xml
<Compile Remove="Info\ScriptSqly.cs" />
```

این فایل از کامپایل این solution خارج شده چون یک **نرم‌افزار جداگانه‌ی
به‌روزرسانی** آن را اجرا می‌کند تا دیتابیس مشتری را با اسکریپت به‌روز کند —
**نه Blazor**. اگر کاربر/صاحب پروژه گفت «من این فایل را با یک برنامه‌ی
دیگر اجرا می‌کنم، از Blazor برای آپدیت دیتابیس استفاده نمی‌کنم»، دقیقاً
همین را می‌گوید؛ سعی نکنید مکانیزم را عوض کنید یا آن را داخل مسیر Blazor
قلاب کنید.

پیامدهای عملی:

* خرابی نحوی در این فایل build این solution را نمی‌شکند — قبل از اعلام
  «باگ کامپایل»، `dotnet build` بگیرید و ببینید واقعاً همین فایل مقصر است.
* هر مهاجرت دیتابیس باید **هم** در `ScriptSqly.cs` باشد **و هم** به‌صورت
  یک فایل `.sql` مستقل در `Server/Database/` تا بشود جدا اجرایش کرد و روی
  دیتابیس تست ساخت. اگر یکی را عوض کردید، دیگری هم باید عوض شود.
* داخل این فایل، اسکریپت‌ها رشته‌ی verbatim (`@"..."`) هستند و روی `GO`
  تکه‌تکه می‌شوند؛ پس `"` باید `""` نوشته شود.

### `Server/Info/*.sql` مستندات‌اند، نه کد زنده

فایل‌هایی مثل `PAY2_Procedures_v6.sql` عکس لحظه‌ای‌اند و می‌توانند از موتور
زنده عقب باشند. **منبع حقیقت `ScriptSqly.cs` (و رویه‌های واقعی دیتابیس)
است، نه این فایل‌ها.** نمونه‌ی واقعی این اختلاف که در همین PR کشف شد:

| | `PAY2_Procedures_v6.sql` (قدیمی) | موتور زنده |
|---|---|---|
| `GROSS_PAY` | ریل اسمی (`BASE_SAL`) | ریل **رسمی** (`BASE_SAL_B`) |
| `NOMINAL_GROSS` | وجود ندارد | ریل اسمی (`BASE_SAL`) |

اگر عددی جور درنیامد یا رفتاری غیرمنتظره دیدید، اول همین اختلاف را چک
کنید — یک بار این PR را با یک «باگ» غلط منحرف کرد تا مشخص شد سند قدیمی
است، نه کد.

### معنای دو ریلِ حقوق (اسمی و رسمی)

نام‌ها گمراه‌کننده‌اند و اگر برعکس فرض شوند، سند و گزارش‌ها غلط می‌شوند:

| ریل | آیتم | یعنی چه |
|---|---|---|
| **اسمی** | `BASE_SAL` | مبلغی که **اعلام** می‌کنیم به شخص می‌دهیم — مبنای بیمه |
| **رسمی** | `BASE_SAL_B` | مبلغی که **واقعاً** به شخص پرداخت می‌شود |

یعنی «رسمی» پرداختِ حقیقی است و «اسمی» رقمِ اعلامی به تأمین اجتماعی —
نه برعکس. پیامدهایش:

* هر چیزی که به **پرداخت** مربوط است (خالص، حساب پرداختنی شخص، هزینه‌ی
  حقوق در سند حسابداری) از ریل **رسمی** می‌آید.
* هر چیزی که به **مشمولیت** مربوط است (`INS_BASE`، مبنای مالیات،
  دیسکت بیمه) از ریل **اسمی** می‌آید.
* این دو می‌توانند خیلی فاصله داشته باشند. نمونه‌ی واقعی تیر ۱۴۰۵:
  یک پرسنل با حقوق پایه‌ی روزانه‌ی اسمی ۹٬۶۱۰٬۶۶۵ و رسمی ۷٬۶۱۰٬۶۶۵ —
  یعنی ماهانه ۶۲ میلیون ریال اختلاف بین «آنچه اعلام شده» و «آنچه پرداخت
  شده». سند باید رقم رسمی را ببرد به هزینه، نه اسمی را.

### دو ریلِ مشمولیت هم مستقل‌اند: بیمه و مالیات

آیتم‌های `SHIFT`، `OT_NORMAL`، `OT_HOLIDAY` و `OT_ADMIN` یک «قاعده‌ی اختصاصی
مشتری» دارند که از یک تاریخ اثر به بعد آن‌ها را از مبنا کنار می‌گذارد. دو کلید
مستقل در `PAY2_CONFIG` کنترلش می‌کنند (صفر = خاموش):

| کلید | چه چیزی را کنار می‌گذارد | چطور ست می‌شود |
|---|---|---|
| `INS_NON_SUBJECT_EFFECTIVE_FROM` | مبنای بیمه | فقط `SP_PAY2_APPLY_INS_NON_SUBJECT_OPTIN` (یک‌بارمصرف، چون `PAY2_ITEM_DEF` را هم بازنویسی می‌کند) |
| `TAX_NON_SUBJECT_EFFECTIVE_FROM` | مبنای مالیات | مثل بقیه‌ی تنظیمات حساس، از صفحه‌ی تنظیمات — چیزی را بازنویسی نمی‌کند و برگشت‌پذیر است |

**تله:** روشن کردنِ تنهای کلید بیمه، مالیات را **زیاد** می‌کند نه کم — چون
مبنای مالیات همان اقلام را نگه می‌دارد ولی بیمه‌ی سهم کارگرِ کسرشدنی از آن
کوچک‌تر می‌شود. نمونه‌ی واقعی تیر ۱۴۰۵ در
`Server/Database/pay2_tax_non_subject_rule.sql` با عدد آمده. اگر مشتری گفت
«اضافه‌کاری و شیفت نباید بیاید»، معمولاً هر دو کلید را می‌خواهد.

هر دو `UPDATE` در `SP_PAY2_CALC_RUN` عمداً **بعد از** حل شدن Overrideها اجرا
می‌شوند، پس `INS_OV=1` / `TAX_OV=1` روی حکمِ قفل‌شده را هم خنثی می‌کنند —
همین باعث می‌شود بشود بدون باز کردن حکمِ تأییدشده مشکل را حل کرد.
`Server/Services/Pay2NonSubjectRule.cs` فقط جلوی ثبتِ Overrideهایی را
می‌گیرد که موتور بی‌صدا نادیده‌شان می‌گیرد.

### `schema.sql` جدول‌های ورود را ندارد

`SALA_DTL` (کاربران)، `SAL_CHEK` (دسترسی فرم‌ها) و `CHARTSAZMANI` در
`schema.sql` و `legacy_dependencies.sql` **نیستند**. دیتابیسی که فقط از
آن‌ها ساخته شود هیچ کاربری ندارد و نمی‌شود واردش شد.
`Server/Database/test_auth_tables.sql` این سه جدول را می‌سازد (باید
**قبل از** `pay2_acl_migration.sql` اجرا شود، چون بخش Bootstrap مهاجرت به
`SALA_DTL` و `SAL_CHEK` ارجاع می‌دهد) و `test_auth_and_acl_users.sql` بعد
از مهاجرت سه کاربر آزمایشی درج می‌کند. ترتیب کامل در بخش ۴.

### یکدستی ظاهری بخش حقوق و دستمزد

همه‌ی تب‌های `Client/Pages/Salary/Tabs/` از یک زبان طراحی مشترک استفاده
می‌کنند و تب یا کامپوننت جدید هم باید همان را دنبال کند:

* `Client/wwwroot/css/pay2-salary.css` — کلاس‌های آماده
  (`pay2-two-panel`، `card-custom`، `p2-table`، `btn-primary-pay2`،
  `p2-empty-state`، `p2-badge-*`، `pay2-search-input` و …)
* آیکون‌ها **Bootstrap Icons** (`<i class="bi bi-...">`)، نه Material
* ورودی‌ها: `Pay2NumericInput` برای مبلغ و عدد و تاریخ شمسی،
  `Pay2Select` برای انتخاب با جستجو، `class="pay2-input"` برای متن ساده
* از `MudTabs` / `MudList` / `MudTextField` و گرید بوت‌استرپ
  (`row` / `col-md-*`) در این بخش استفاده نکنید

### دو باگ ظریف CSS که پیدا و رفع شدند — الگویشان را در جای دیگر تکرار نکنید

۱. **قوانین `!important` روی `animation`/`opacity` می‌توانند رفتار
   کتابخانه‌ها را بی‌صدا خراب کنند.** `MudSnackbarElement` برای بستن هر
   toast، انیمیشن محو شدنِ خودش را با `style` این‌لاین (`animation: …ms
   linear snackbar-<guid>;`) تنظیم می‌کند. یک قانون سراسری
   `.mud-snackbar { animation: … !important; }` که برای زیبایی ورودی
   نوشته شده بود، همیشه روی همین این‌لاین برنده می‌شد و **محو شدن را کاملاً
   بی‌اثر می‌کرد** — کلیک روی × ثبت می‌شد ولی هیچ فیدبک بصری‌ای نبود تا دو
   ثانیه بعد که ناگهان ناپدید می‌شد. قبل از هر `!important` روی یک پراپرتی
   که یک کامپوننت شخص‌ثالث (MudBlazor، Syncfusion، …) خودش هم روی همان
   عنصر تنظیم می‌کند، فکر کنید که آیا آن کامپوننت در حالت دیگری (بسته
   شدن، خطا، لودینگ) هم به همان پراپرتی نیاز دارد یا نه.
2. `btn-ghost-pay2` هیچ استایل `:disabled` نداشت — دکمه‌ی غیرفعال دقیقاً
   مثل دکمه‌ی فعال به‌نظر می‌رسید. هر دکمه‌ی جدید با ویژگی `disabled`
   پویا، باید در CSS هم حالت غیرفعالش تعریف شده باشد؛ وگرنه رد شدن یک
   عملیات شبیه خرابی برنامه به‌نظر می‌رسد نه شبیه «اجازه نداری».

### وقتی رفتار یک کتابخانه‌ی شخص‌ثالث نامشخص است، decompile کنید

وقتی رفتار داخلی MudBlazor (بالا) مبهم بود، منبعش را از روی DLL کامپایل‌شده
بازخوانی کردم به‌جای حدس زدن:

```bash
dotnet tool install -g ilspycmd --version 9.1.0.7988   # نسخه‌ای که با runtime نصب‌شده سازگار است
export PATH="$PATH:/root/.dotnet/tools"
ilspycmd -l c path/to/Package.dll | grep -i "TypeName"   # فهرست انواع
ilspycmd -t "Namespace.TypeName" path/to/Package.dll     # سورس بازسازی‌شده‌ی یک نوع
```

نکته: نسخه‌ی `ilspycmd` باید با runtime نصب‌شده هم‌خوان باشد (اینجا فقط
.NET 8 runtime بود، پس نسخه‌ی ۹ کار کرد و نسخه‌ی ۸ نه). این خیلی سریع‌تر و
دقیق‌تر از آزمایش‌وخطا با رفتار runtime است، مخصوصاً برای کتابخانه‌های
UI که رفتارشان به state داخلی و تایمر وابسته است (مثل انیمیشن‌های
Snackbar که فقط با گذشت زمان قابل مشاهده‌اند).

### برچسب‌های فرم به ورودی‌هایشان وصل نیستند

MudBlazor 6.21.0 برچسب را با `for="mudinput-{guid}"` می‌سازد ولی هیچ `id`
ای روی خود `<input>` نمی‌گذارد. نتیجه: هیچ‌کدام از `label[for]`های برنامه
به عنصری اشاره نمی‌کنند که وجود داشته باشد (روی صفحه ورود: ۴ برچسب، ۰
ورودی دارای id).

اثرش دو چیز است:

* **دسترس‌پذیری** — صفحه‌خوان نمی‌تواند بگوید هر فیلد چیست، و کلیک روی
  برچسب فوکوس را به ورودی نمی‌برد. این روی **همه‌ی فرم‌های برنامه** اثر
  دارد؛ رفعش (ارتقای MudBlazor یا `id` دستی روی هر فیلد) دامنه‌ی گسترده
  دارد، بدون هماهنگی با صاحب پروژه انجامش ندهید.
* **تست** — `page.getByLabel('نام کاربری')` کار نمی‌کند. از کمکی
  `field(page, 'برچسب')` در `Tests/e2e/helpers/app.js` استفاده کنید.

### رمز عبور در دیتابیس کدشده است، نه هش — عمدی است

`Shared/Utility/CL_METHODS.cs` → `DECODEUN` (نام کاربری، +۲۰ روی بایت‌های
CP1256) و `DECODEPS` (رمز، +۱۰ و بعد حذف ۳ کاراکتر از هر طرف).
این رمزنگاری را نرم‌افزار WPF قدیمی تعیین کرده؛ **این مخزن حق ندارد آن را
به هش تبدیل کند** بدون هماهنگی، چون نرم‌افزار دیگر دیگر نمی‌تواند رمز را
بخواند. برای ساختن کاربر تست باید مقدار کدشده را با همین توابع حساب کنید؛
مقادیر `test_auth_and_acl_users.sql` با اجرای واقعی همین توابع
راستی‌آزمایی شده‌اند.

### `ENABL=0` یعنی «فعال» در برخی جاها — قصدی است، تناقض نیست

`SALA_DTL.ENABL`: ورود (`AuthController`) و `LookupController` از
`ENABL=0` به‌عنوان «کاربر فعال» استفاده می‌کنند. اگر جای دیگری از کد همین
فیلتر را برعکس (`ENABL=1`) به‌کار برد، **قبل از «اصلاحش» با صاحب پروژه
چک کنید** — در همین PR یک‌بار این فرض اشتباه گرفته شد و کاملاً بایت‌به‌بایت
برگردانده شد (`646e228`). صفحه‌ی مدیریت دسترسی‌ها عمداً `ENABL` را فیلتر
نمی‌کند چون باید بتوان دسترسی کاربرانی را هم که موقتاً غیرفعال‌اند از قبل
تنظیم کرد.

---

## ۳. کنترل دسترسی حقوق و دستمزد (PAY2 ACL)

دو لایه دارد:

1. **دسترسی فرم** — `TFORMS` (تعریف فرم) + `SAL_CHEK` (مجوز هر کاربر).
   پنج مجوز بیتی: `RUN=1`، `SEE=2`، `INP=4`، `UPD=8`، `DEL=16`.
2. **محدوده کارگاه** — `PAY2_USER_WS` مشخص می‌کند هر کاربر کدام
   کارگاه‌ها را می‌بیند. **لیست‌ها هم بر همین اساس فیلتر می‌شوند، نه فقط
   دکمه‌ها.** استثنا: صفحه‌ی خودِ «مدیریت دسترسی‌ها»
   (`GET /api/pay2/access/workshops`) عمداً همه‌ی کارگاه‌ها را نشان
   می‌دهد — وگرنه مدیری که هنوز کارگاهی به خودش تخصیص نداده، فهرست خالی
   می‌بیند و نمی‌تواند حتی به خودش هم کارگاه بدهد (قفل خودساخته).

دو کلید در `PAY2_CONFIG`:

* `ACL_ENFORCE` — پیش‌فرض `'0'` (خاموش) تا نصب روی مشتری چیزی را قطع
  نکند. `Pay2AccessDto.Has()` وقتی این خاموش است بی‌قیدوشرط `true`
  برمی‌گرداند.
* `ACL_WS_SCOPE_ENFORCE` — پیش‌فرض `'1'`.

تنظیمات ACL با TTL کوتاه کش می‌شوند (`Pay2AccessService`). بعد از هر
ذخیره‌ی تنظیمات، `IPay2AccessService.InvalidateConfigAsync()` باید صدا
زده شود — وگرنه روشن/خاموش کردن `ACL_ENFORCE` تا سر رسیدن TTL بی‌اثر
به‌نظر می‌رسد.

### دو ریل مجوز: اتریبیوت و بررسی داخل بدنه

بیشتر اکشن‌ها `[Pay2Authorize(Pay2Forms.X, Pay2Perm.Y)]` دارند. چند اکشن
مجوزشان به داده‌ی ورودی بستگی دارد (مثلاً ساخت کارگاه `Inp` می‌خواهد،
ویرایش `Upd`) و ناچار داخل بدنه بررسی می‌کنند. برای آن‌ها **حتماً** از
`HasAndAuditAsync(HttpContext, userCo, form, perm)` استفاده کنید نه
`HasAsync` خالی: دومی تصمیم می‌گیرد ولی چیزی در `PAY2_SEC_AUDIT` نمی‌نویسد،
یعنی تلاش ناموفق روی حساس‌ترین عملیات‌ها بی‌رد می‌ماند.

```csharp
[HttpPost("save")]
[Pay2Authorize(Pay2Forms.Employee, Pay2Perm.Inp)]
public async Task<IActionResult> Save(...) { ... }
```

`.github/scripts/check_pay2_acl.py` در CI اجرا می‌شود و قرمز می‌کند اگر:
اکشنی در `Server/Controllers/Pay2*.cs` بدون `[Pay2Authorize]` (و بدون
`HasAndAuditAsync` در بدنه) بماند، `[AllowAnonymous]` بگیرد، یا از
`.HasAsync(` بدون جفت `Audit` استفاده کند. استثناها باید در `ALLOWLIST`
همان فایل با دلیل ثبت شوند — بدون دلیل مستند اضافه نکنید.

### فرم‌های فرعی/عمل‌محور (Pseudo-forms)

عملیات حساسی که «فرم» به‌معنای معمول نیستند (تأیید حکم، صدور سند، تغییر
تنظیمات حیاتی، …) هم در `TFORMS` به‌عنوان `PAY2_ACT_*` ثبت شده‌اند تا
بشود مجوزشان را جدا از فرم اصلی کنترل کرد. لیست کامل در
`Shared/Constants/Pay2Forms.cs`.

---

## ۳-۲. کنترل دسترسی CRM

عمداً **خیلی ساده‌تر از PAY2 ACL** است و فقط یک سؤال را جواب می‌دهد: این
کاربر داده‌های همه را می‌بیند یا فقط داده‌های خودش را؟ سطح‌بندی
RUN/SEE/INP/UPD/DEL اینجا وجود ندارد؛ اگر خواستید اضافه کنید، اول با صاحب
پروژه چک کنید — این سادگی یک انتخاب صریح بوده، نه چیزی که جا افتاده باشد.

| قطعه | نقش |
|---|---|
| `PAY2_CONFIG.CRM_ACL_ENFORCE` | شیر اصلی. `'0'` = خاموش (پیش‌فرض) |
| فرم `CRMALL` در `TFORMS` | هر کاربری که `RUN` آن را دارد، همه را می‌بیند |
| `Server/Services/CrmAccessService.cs` | این دو را می‌خواند و `CrmAccessDto` می‌سازد |
| `CrmAccessDto.RestrictToOwn` | `Enforced && !CanSeeAllUsers` — تنها تصمیمی که کنترلر می‌گیرد |

`CRMALL` یک فرم واقعی نیست، یک کلید روشن/خاموش per-user است — همان الگوی
`CUSTEN` و `AZADPAY` که در `Client/Pages/Kharid/ItemGroups.razor` هم
استفاده شده. تنظیمش از صفحه‌ی دسترسی‌های **نرم‌افزار WPF** انجام می‌شود، نه
از Safir.

### دو تله

۱. **مالکیت با `userid` است، نه `USER_NAME`.** ولی رکوردهایی که نرم‌افزار
   WPF می‌سازد `userid` ندارند و فقط `USER_NAME` دارند (روی دیتابیس مشتری
   ۶ شرکت و ۷ پیگیری، آخری آبان ۱۴۰۴ — یعنی هنوز هم ساخته می‌شوند). برای
   همین شرط مالکیت دو شاخه دارد:

   ```sql
   userid = @AclUserId OR (userid IS NULL AND USER_NAME = @AclUserName)
   ```

   معادلش در `CRMEVENTS` ستون `SALER` است. `OwnCompanyPredicate` و
   `OwnEventPredicate` در `CrmController` همین را می‌سازند — شرط مالکیت را
   دستی جای دیگری ننویسید.

۲. **مالکیت هنگام ویرایش جابه‌جا نمی‌شود.** `SaveCompany` و `SaveEvent`
   مالک را از دیتابیس می‌خوانند و برمی‌گردانند، و رکوردی که فقط
   `USER_NAME`/`SALER` دارد را عمداً بدون `userid` رها می‌کنند. بدون این،
   ویرایشِ یک مدیرِ دارای `CRMALL` رکورد را بی‌صدا از مالک واقعی‌اش می‌گرفت.
   `USERID` هم دیگر از بدنه‌ی درخواست پذیرفته نمی‌شود.

### آنچه عمداً محدود **نشده**

* `check-duplicate` — باید سراسری بماند، وگرنه دو فروشنده روی یک سرنخ کار
  موازی می‌کنند. نام مالک را لو نمی‌دهد، فقط وجود تکرار را می‌گوید.
* نیمه‌ی «طرف‌حساب» دفترچه تلفن (`CUST_HESAB`) — داده‌ی مشترک حسابداری است
  و اصلاً ستون مالکیت ندارد. نیمه‌ی «شرکت CRM» فیلتر می‌شود.
* `distinct-salers` / `distinct-buyers` / `send-sms`.

### مهاجرت

`Server/Database/crm_acl_migration.sql` و متد `CrmAclScript` در
`ScriptSqly.Core/ScriptSqly.Blazor.cs` (مخزن جدا، submodule روی
`External/ScriptSqly`). این دو باید مو‌به‌مو یکی بمانند.

**نرم‌افزار WPF این محدودیت را اعمال نمی‌کند.** بستن Safir جلوی دسترسی از
طریق WPF را نمی‌گیرد؛ این محدودیتِ شناخته‌شده است، نه باگ.

---

## ۴. راه‌اندازی محیط تست — با هر سطح دسترسی شبکه

هدف نهایی: یک SQL Server **واقعی و محلی**، دیتابیس ساخته‌شده از صفر، و
برنامه‌ای که واقعاً به آن وصل است. تست‌های شبیه‌سازی‌شده (in-memory) ارزش
خودشان را دارند ولی **سه نقص واقعی در همین PR فقط با دیتابیس واقعی پیدا
شدند** (ترتیب اجرای اسکریپت‌ها، طول رمز کدشده، و payload اشتباه یک تست) —
پس اگر تغییرتان به دیتابیس یا مهاجرت‌ها مربوط است، محیط شبیه‌سازی‌شده
کافی نیست.

**قبل از اجرای هر مسیر پایین: اگر کاربر/صاحب پروژه گفته «هر بار لازم
نیست تست دیتابیس بگیری، چون زمان و توکن زیادی مصرف می‌کند، اول اجازه
بگیر» — این دستور برای همه‌ی جلسات بعدی هم معتبر است. فقط وقتی تغییر واقعاً
به رفتار دیتابیس/مهاجرت وابسته است پیشنهاد بدهید، و قبل از واقعاً بالا
آوردن SQL Server یک‌بار صریح تأیید بگیرید.**

### مسیر ۱ (پیش‌فرض) — اسکریپت آماده

```bash
export ConnectionStrings__DefaultConnection='Server=localhost,1433;Database=SafirTestDb;User Id=sa;Password=...;TrustServerCertificate=True'
export Jwt__Key='یک-رشته-تصادفی-حداقل-۳۲-بایتی'
./scripts/setup-test-env.sh
```

این اسکریپت به‌ترتیب: .NET 8 را تضمین می‌کند → SQL Server را نصب/بالا
می‌آورد (`apt` از `packages.microsoft.com`) → دیتابیس `SafirTest*` را از نو
می‌سازد → اسکریپت‌ها را به این ترتیب دقیق اجرا می‌کند:

```
legacy_dependencies.sql → schema.sql → test_auth_tables.sql
  → pay2_runtime_procedures.sql → pay2_seed.sql → pay2_acl_migration.sql
  → test_auth_and_acl_users.sql
```

**این ترتیب پای هر تغییری که در این اسکریپت‌ها می‌دهید حفظ شود:**
`pay2_seed.sql` کل `PAY2_CONFIG` را `DELETE` و دوباره پر می‌کند، پس
مهاجرت ACL باید **بعد از** seed اجرا شود، وگرنه کلیدهای `ACL_ENFORCE`
پاک می‌شوند و کنترل دسترسی بی‌صدا خاموش می‌ماند.

گاردهای ایمنی اسکریپت: فقط SQL Server روی `localhost` و فقط نام دیتابیسی
که با `SafirTest` شروع شود (چون دیتابیس را `DROP` می‌کند). این گاردها را
دور نزنید.

کاربران آزمایشی که در انتها ساخته می‌شوند:

| نام کاربری | رمز | نقش |
|---|---|---|
| `payadmin` | `111111` | دسترسی کامل، همه کارگاه‌ها |
| `payviewer` | `222222` | فقط `RUN`+`SEE` روی همه‌ی فرم‌ها، و **کاملاً بدون دسترسی** به `PAY2_DECREE` (برای تست حالت «اصلاً دسترسی ندارد»، نه فقط «فقط‌خواندنی») |
| `payscoped` | `333333` | دسترسی کامل، فقط کارگاه ۱ (کارگاه دوم فقط برای همین تست ساخته می‌شود) |

### مسیر ۲ — وقتی `apt`/`packages.microsoft.com` بسته است ولی Docker هست

بعضی محیط‌های ایزوله (شبیه sandbox این عامل) دسترسی به مخازن بسته‌ی
مایکروسافت را می‌بندند ولی رجیستری کانتینر باز است.

1. اول واقعاً چک کنید Docker چیست، نه فرض کنید «در دسترس نیست»:
   ```bash
   docker version          # اگر دستور هست ولی خطا می‌دهد، مرحله‌ی بعد
   sudo systemctl start docker || sudo dockerd &   # اگر service بالا نیامد (مثلاً به‌خاطر ulimit)، مستقیم dockerd را اجرا کنید
   ```
2. `docker.io` (Docker Hub) ممکن است پشت CDN مسدود باشد (۴۰۳)، ولی
   `mcr.microsoft.com` معمولاً باز است:
   ```bash
   docker pull mcr.microsoft.com/mssql/server:2022-latest
   docker run -d --name safir-sql -p 1433:1433 \
     -e ACCEPT_EULA=Y -e MSSQL_PID=Developer \
     -e MSSQL_SA_PASSWORD='...' \
     -e MSSQL_MEMORY_LIMIT_MB=3072 \
     mcr.microsoft.com/mssql/server:2022-latest
   ```
   `MSSQL_MEMORY_LIMIT_MB` را در محیط‌های کم‌حافظه صریح بدهید — بدون آن
   دیده شده سرویس در همان اولین اتصال `sqlcmd` کرش می‌کند.
3. بعد از این، بقیه‌ی مسیر همان مسیر ۱ است: `sqlcmd` را نصب کنید
   (یا از `docker exec` روی همان کانتینر استفاده کنید) و اسکریپت‌ها را
   به همان ترتیب بالا اجرا کنید.

### مسیر ۳ — وقتی نه `apt` و نه Docker در دسترس‌اند

این وضعیت فرضی نیست: در همین PR، هر دو مسیر بسته بودند (دانلود بسته با
۴۰۳ رد می‌شد، Docker هم قابل استفاده نبود در آن لحظه). راه‌حل: همان
Registry HTTP API که Docker خودش پشت صحنه استفاده می‌کند را مستقیم با
`curl` صدا بزنید — بدون نصب Docker.

**روش کلی:**

1. **گرفتن توکن ناشناس** از رجیستری برای `mcr.microsoft.com` (پاسخ ۴۰۱
   اولیه، هدر `WWW-Authenticate` آدرس سرویس توکن و scope لازم را می‌دهد؛
   با `curl` به همان آدرس با همان scope درخواست بزنید تا یک Bearer token
   بگیرید — بدون نیاز به لاگین، چون ایمیج‌های عمومی مایکروسافت باز است).
2. **گرفتن manifest** ایمیج (`GET /v2/mssql/server/manifests/2022-latest`
   با هدر `Accept: application/vnd.docker.distribution.manifest.list.v2+json`)
   و از آن، manifest مربوط به `linux/amd64` را انتخاب کنید.
3. **دانلود لایه‌ها** (`GET /v2/mssql/server/blobs/<digest>` با همان
   Bearer token) — هرکدام یک `tar.gz` است.
4. **استخراج لایه‌ها به‌ترتیب** روی هم، در یک پوشه‌ی ریشه‌ی جدا (مثلاً
   `/opt/sqlroot`) — دقیقاً همان کاری که Docker با overlay filesystem
   می‌کند، فقط اینجا مسطح روی دیسک.
5. **اجرا زیر `chroot`** به‌جای کانتینر:
   ```bash
   sudo mount --bind /proc /opt/sqlroot/proc
   sudo mount --bind /sys  /opt/sqlroot/sys
   sudo mount --bind /dev  /opt/sqlroot/dev
   sudo chroot /opt/sqlroot /opt/mssql/bin/sqlservr &
   ```
   با متغیرهای محیطی لازم (`ACCEPT_EULA=Y`، `MSSQL_SA_PASSWORD`،
   `MSSQL_PID=Developer`، `MSSQL_TCP_PORT=1433`،
   `MSSQL_MEMORY_LIMIT_MB=3072`) پیش از اجرای `chroot` ست‌شده باشند
   (`env VAR=... chroot ...` یا export قبل از دستور).

**نکته‌ی مهم عملی:** اگر ابزار اجرای دستورهایتان هر فراخوانی را در یک
پردازه/namespace جدا اجرا می‌کند (مثل بعضی هارنس‌های عامل هوش مصنوعی)،
**bind mountها بین دو فراخوانی جدا از بین می‌روند.** راه‌حل: مرحله‌ی
mount کردن و اجرای `sqlservr` را در **یک اسکریپت واحد** بنویسید و آن
اسکریپت را یک‌بار با `&`/`nohup` در پس‌زمینه اجرا کنید، نه اینکه mount را
در یک دستور و اجرا را در دستور بعدی انجام دهید.

بعد از بالا آمدن (چند ثانیه تا چند دقیقه، بسته به حافظه)، `sqlcmd` را از
بیرون (نصب‌شده روی هاست، نه داخل chroot) به `localhost,1433` وصل کنید و
همان ترتیب اسکریپت‌های مسیر ۱ را اجرا کنید.

### تأیید اینکه SQL Server واقعاً کار می‌کند

قبل از رفتن سراغ اسکریپت‌ها، این را چک کنید:

```bash
/opt/mssql-tools18/bin/sqlcmd -S localhost,1433 -U sa -P '...' -C -Q "SELECT @@VERSION;"
```

### آنچه بدون دیتابیس هم قابل تست است

برنامه بدون دیتابیس بالا می‌آید — Dapper فقط موقع اجرای کوئری وصل
می‌شود. این‌ها همیشه قابل اجرا هستند و در CI هم اجرا می‌شوند (بدون هیچ
SQL Server ای — `.github/workflows/build.yml` را ببینید):

* `dotnet test Safir.sln` — تست‌های واحد: فیلتر واقعی `[Pay2Authorize]`،
  ریاضی بیتی مجوزها، تست‌های سرتاسری روی HTTP با دیتابیس درون‌حافظه‌ای
  (`Tests/Safir.Server.Tests/InMemoryDatabase.cs`)، و رفتار
  `DelegatingHandler`های سمت کلاینت.
* `Tests/e2e/tests/01-smoke.spec.js` — تست‌های مرورگر واقعی که به
  دیتابیس نیاز ندارند (بوت WebAssembly، ۴۰۱ روی endpointهای محافظت‌شده،
  رفتار toast).

تست‌هایی که به دیتابیس نیاز دارند (`02-acl.spec.js`) خودشان `skip`
می‌شوند، نه `fail`، اگر دیتابیس در دسترس نباشد. شکستِ محیط را با شکستِ
برنامه قاطی نکنید — و به همین دلیل، اگر یکی از این تست‌ها skip شد، این
به‌معنای «سبز» نیست؛ یعنی هنوز آزمایش نشده.

### آزمون دود سطحی ننویسید

`/` در Blazor WebAssembly یک فایل استاتیک است و **حتی وقتی دیتابیس و کل
API خراب باشد هم ۲۰۰ برمی‌گرداند**. سه چیز را جدا بررسی کنید:

1. `GET /_framework/blazor.boot.json` → کلاینت واقعاً بالا می‌آید
2. `POST /api/auth/login` → دیتابیس، Dapper، JWT و کدگشایی رمز سالم‌اند
3. `GET /api/pay2/access/me` بدون توکن ۴۰۱ و با توکن ۲۰۰ → ACL فعال است

### تست انسانی با Playwright

بعد از بالا آمدن برنامه روی `http://127.0.0.1:5080`، با Playwright وارد
شوید و همان مسیرهایی را بروید که کاربر می‌رود — نه فقط API را صدا بزنید.
ارزشمندترین سناریو مقایسه‌ی سه کاربر بالاست: `payviewer` نباید دکمه‌ی
ذخیره ببیند و اگر مستقیم API را صدا بزند باید ۴۰۳ بگیرد (با پیام فارسیِ
سرور، نه متن خام HTTP)؛ `payscoped` نباید کارگاه‌های دیگر را در لیست
ببیند. برای بررسی سریع یک فرضیه بدون نوشتن یک فایل تست کامل، می‌شود یک
اسکریپت Playwright یک‌بارمصرف نوشت (مثل آنچه برای اثبات باگ Snackbar در
همین PR استفاده شد) — ولی قبل از پایان کار، آن اسکریپت‌های یک‌بارمصرف را
پاک کنید یا به یک تست دائمی در `Tests/e2e/tests/` تبدیل کنید؛ چیزی روی
دیسک از آن‌ها باقی نماند.

---

## ۵. تعریف «تحویل آماده‌ی Production» برای این پروژه

قبل از اعلام اینکه یک تغییر تمام است، همه‌ی این‌ها را واقعاً اجرا کنید،
نه فقط فرض کنید که رد می‌شوند:

```
dotnet build Safir.sln -c Debug                    ← ۰ خطا
dotnet build Safir.sln -c Release                  ← ۰ خطا (CI با Release می‌سازد)
dotnet test  Safir.sln                             ← همه سبز
python3 .github/scripts/check_pay2_acl.py          ← هر اکشن PAY2 محافظت‌شده
cd Tests/e2e && npx playwright test 01-smoke.spec.js  ← بدون دیتابیس، همیشه باید سبز باشد
```

اگر تغییرتان به دیتابیس/مهاجرت/ACL مربوط است، اضافه بر بالا (با اجازه‌ی
صریح صاحب پروژه، طبق بخش ۴):

```
./scripts/setup-test-env.sh                        ← دیتابیس واقعی از صفر ساخته شود
cd Tests/e2e && npx playwright test 02-acl.spec.js ← با سه کاربر واقعی
```

و علاوه بر تست‌های خودکار:

* **رگرسیون تست بنویسید، نه فقط رفع کنید.** هر باگی که پیدا می‌کنید باید
  یک تست جدید هم داشته باشد که اگر برگردد، قرمز شود. تست‌های
  `Pay2ForbiddenHandlerTests`، `Pay2AccessCacheTests`، و تست Snackbar در
  `01-smoke.spec.js` نمونه‌ی این الگو هستند.
* **دامنه‌ی تغییر را محدود نگه دارید.** اگر یک commit دو موضوع نامرتبط را
  با هم عوض کند (مثلاً یک رفتار عمدی را «اصلاح» و یک باگ واقعی را رفع)،
  برگرداندن اشتباه بعداً باعث می‌شود رفع باگ واقعی هم گم شود — این دقیقاً
  در همین PR یک‌بار اتفاق افتاد.
* **پیام‌های خطا را از دید کاربر نهایی بخوانید.** `ex.Message` خام یا
  کد وضعیت HTTP بدون توضیح، برای کاربری که فارسی می‌خواند بی‌معنی است.
  اگر سرور پیام روشن می‌دهد، کلاینت باید همان را نشان دهد، نه پیام
  عمومی `.NET`.
* **قبل از commit، `git diff` را کامل بخوانید** — نه فقط فایل‌هایی که
  فکر می‌کنید عوض کرده‌اید. تغییرات ناخواسته (یک فرمت‌کننده که فاصله‌ها
  را عوض کرده، یک فایل تستِ یک‌بارمصرف که جا مانده) اینجا گرفته می‌شود.
* **کامیت‌های کوچک و توصیفی.** هر کامیت باید به‌تنهایی قابل‌فهم باشد:
  چه چیزی، چرا، و چطور تأیید شد. پیام‌های کامیت این PR (`git log`) الگوی
  خوبی‌اند.

### چک‌لیست سریع پیش از گفتن «تمام شد»

- [ ] `dotnet build` (Debug و Release) بدون خطا
- [ ] `dotnet test` سبز
- [ ] نگهبان ACL سبز (اگر کنترلر PAY2 دست خورده)
- [ ] تست‌های Playwright بدون دیتابیس سبز
- [ ] اگر دیتابیس/مهاجرت دست خورده: با اجازه، تست روی SQL Server واقعی
- [ ] رگرسیون‌تست برای هر باگی که رفع شده
- [ ] `git diff` کامل خوانده شده، چیزی ناخواسته نیست
- [ ] پیام کامیت توضیح می‌دهد چرا، نه فقط چه چیزی

---

## ۶. راستی‌آزمایی محاسبات حقوق

```bash
# در SSMS: Server/Tools/pay2_export.sql را اجرا و نتیجه را ذخیره کنید
python3 Server/Tools/pay2_audit.py pay2_export.json
```

معادله‌های موتور محاسبه را روی داده‌ی واقعی بررسی می‌کند بدون اتصال به
دیتابیس. جزئیات کامل در `Server/Tools/README.md`.

نکته‌ای که این ابزار پیدا کرده و باید به‌عنوان یک قاعده‌ی کسب‌وکار دانست،
نه باگ: در `PAY2_ATTENDANCE`، الگوی `DAYS>0, DAYSB=0` یعنی پرسنلی که
حقوق پرداخت نمی‌شود ولی بیمه‌اش کامل رد می‌شود (خالص نهایی منفی، عمدی و
مجاز). فقط الگوی معکوسش (`DAYSB>0, DAYS=0`) واقعاً نشانه‌ی خرابی داده است.

---

## ۷. نکات امنیتی شناخته‌شده (عمداً رفع نشده — دامنه‌اش با صاحب پروژه است)

* `Server/Controllers/AuthController.cs` — یک رمز عبور دورزننده
  (`bypassLogin`) روی همه‌ی کاربرها کار می‌کند. عمدی بوده (احتمالاً برای
  پشتیبانی) ولی در محیط واقعی ریسک جدی است.
* رمزها هش نشده‌اند، فقط کدشده‌اند (بخش ۲).
* قطعی دیتابیس به کاربر «رمز اشتباه» گزارش می‌شود چون خطای اتصال بلعیده
  می‌شود — موقع نصب، دنبال رمز می‌گردید در حالی که مشکل دیتابیس است.
* لاگ خطا به مسیر ویندوزیِ هاردکد `C:\CORRECT\Blazor_ErDbms_Log.txt`
  نوشته می‌شود؛ روی لینوکس فایلی با همین نام در پوشه‌ی کاری می‌سازد.
  داخل `try/catch` است پس کرش نمی‌کند، ولی لاگ گم می‌شود.
* خروجی `pay2_export.json` (بخش ۶) داده‌ی واقعی پرسنل دارد؛ در
  `.gitignore` است و نباید commit شود.

این‌ها را رفع نکنید مگر صریحاً خواسته شود — فقط بدانید وجود دارند تا
اشتباهی به‌عنوان اثر جانبیِ یک تغییر دیگر رفعشان نکنید یا رفتارشان را
به‌عنوان «باگ جدید» گزارش ندهید.

---

## ۸. سبک کار

* پیام‌های خطای رو به کاربر فارسی‌اند — همین را ادامه دهید.
* کامنت‌ها در بخش حقوق و دستمزد فارسی‌اند؛ در بقیه‌ی کد انگلیسی. از سبک
  فایلی که ویرایش می‌کنید تقلید کنید.
* قبل از اعلام «باگ»، `dotnet build` بگیرید و بخش ۲ را دوباره چک کنید —
  بیشتر «باگ»های ظاهری این مخزن یا تله‌های مستندشده‌اند یا رفتار عمدی.
* «Over-engineering نکنید» یک دستور تکرارشونده از صاحب پروژه است: رفع هر
  مشکل باید کوچک‌ترین تغییری باشد که آن مشکل مشخص را حل می‌کند، نه
  بازطراحی چیزی که کار می‌کند.

---

## ۹. موتور مهاجرت و مدیریت دیتابیس (ScriptSqly)

* **پایان فایل‌های مرده**: فایل `ScriptSqly.cs` دیگر یک فایل مرده در مخزن Safir نیست و کاملاً پاکسازی شد.
* **کتابخانه مشترک**: منطق آپدیت دیتابیس (Migration) به پروژه مشترک `ScriptSqly` منتقل شده است.
* **راهنمای هوش مصنوعی (محل کدهای اسکریپت)**:
  - **هوش محلی (Local AI):** کدهای اسکریپت را از مسیر محلی `E:\prg\ScriptSqly\ScriptSqly.Core` بخوانید.
  - **هوش ابری (Cloud AI / Jules / Copilot):** کدهای اسکریپت را از پوشه ساب‌ماژول `External/ScriptSqly/ScriptSqly.Core` بخوانید.

/* ═══════════════════════════════════════════════════════════════════
   TAGCOD.tartib — پرکردن خودکار وقتی خالی است

   ── چرا لازم شد ──
   tartib ترتیبِ پردازشِ انواع برگه در یک روز را می‌دهد و پایه‌ی
   محاسبه‌ی موجودی تجمعی در CHK-01 (کاردکس منفی) و رتبه‌بندی نرخ در
   CHK-02 است. ولی داده‌ی پایه‌ی هر شرکت است و هیچ‌جا ساخته نمی‌شد.

   روی شرکتِ «پودر مروارید» (newpoodr1405) این ستون برای همه‌ی انواع
   برگه NULL بود. نتیجه‌اش کاردکس منفیِ کاذب شد: کد ۹۵۶ (شیر اسکیم)،
   انبار ۷، ۱۴۰۵/۰۱/۰۱ — حواله خروج شماره ۱ قبل از انتقالی-ورود شماره ۲
   پردازش می‌شد و مانده را ۳۱٬۶۲۵− نشان می‌داد، در حالی که کارت کالای
   واقعی همان روز صفر است.

   ── مقادیر از کجا آمده‌اند ──
   عیناً از یک پایگاه سالم که این ستون در آن پر است. اعتبارش را خودِ
   داده تأیید می‌کند: ترتیبِ این اعداد دقیقاً همان ترتیبِ الفباییِ
   BARGAH *با فاصله‌های ابتدایی* است — یعنی همان مکانیزمی که سیستم
   قدیمی برای ترتیب استفاده می‌کند و گزارش‌های مرجع dbo.KA_KH و
   dbo.MOGHA_ANBAR بر پایه‌ی آن کار می‌کنند. پس این دو منبع مستقل یک
   جواب می‌دهند، و آنچه اینجا نوشته می‌شود همان چیزی است که کارت کالای
   واقعی نشان می‌دهد.

   ── محافظه‌کاری ──
   فقط ردیف‌هایی که tartib آنها NULL است پر می‌شوند. اگر شرکتی این ستون
   را خودش (حتی متفاوت) تنظیم کرده باشد، دست نمی‌خورد — ترتیبِ برگه‌ها
   تصمیمِ آن شرکت است، نه تصمیمِ ما.

   ── بدون هشدار ──
   طبق تصمیم صاحب پروژه، این وضعیت استثنا تولید نمی‌کند؛ فقط درست
   می‌شود. کاربر قرار نیست بابت داده‌ای که اصلاً به او تحویل داده نشده
   پیام بگیرد.

   نکته: عمداً هیچ «USE <database>» اینجا نیست.
   ═══════════════════════════════════════════════════════════════════ */

SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

IF COL_LENGTH('dbo.TAGCOD', 'tartib') IS NOT NULL
BEGIN
    ;WITH Marja (CODE, tartib) AS (
        SELECT * FROM (VALUES
            ( 0,  1),   -- موجودی اول دوره
            (12,  2),   -- خرید
            ( 7,  3),   -- تولید-ورود
            ( 1,  4),   -- رسید خرید
            (17,  5),   -- کسری انبار
            ( 4,  6),   -- برگشت فروش
            (22,  7),   -- برگشت فروش (مقدار مرجوعی)
            (24,  8),   -- برگشت فروش
            ( 9,  9),   -- حواله ورود
            ( 6, 10),   -- انتقالی - ورود
            (26, 11),   -- برگشت خرید آزاد
            (27, 12),   -- فاکتور برگشت خرید آزاد
            (18, 13),   -- اضافه انبار
            ( 5, 14),   -- انتقالی - خروج
            ( 3, 15),   -- برگشت خرید
            (20, 16),   -- پیش‌فاکتور
            ( 8, 17),   -- تولید - خروج
            ( 2, 18),   -- حواله فروش
            (10, 19),   -- حواله خروج
            (11, 20),   -- حواله خروج سایر
            (14, 21),   -- فروش
            (15, 22),   -- سایر
            (13, 23)    -- فروش
        ) v (CODE, tartib)
    )
    UPDATE  t
    SET     t.tartib = m.tartib
    FROM    dbo.TAGCOD t
    JOIN    Marja m ON m.CODE = t.CODE
    WHERE   t.tartib IS NULL;

    /* نوع برگه‌ای که در فهرست مرجع نیست ولی در این پایگاه وجود دارد.
       ترتیبش از خودِ BARGAH می‌آید — همان مکانیزمِ سیستم قدیمی — و بعد
       از همه‌ی ردیف‌های شناخته‌شده می‌نشیند، تا ترتیبِ آنها را به‌هم
       نزند.

       ⚠️ BARGAH عمداً LTRIM نمی‌شود: فاصله‌های ابتدایی خودشان حاملِ
       ترتیب‌اند. */
    IF EXISTS (SELECT 1 FROM dbo.TAGCOD WHERE tartib IS NULL)
    BEGIN
        DECLARE @Max INT = ISNULL((SELECT MAX(tartib) FROM dbo.TAGCOD), 0);

        ;WITH Baghi AS (
            SELECT  CODE, tartib,
                    rn = ROW_NUMBER() OVER (ORDER BY BARGAH, CODE)
            FROM    dbo.TAGCOD
            WHERE   tartib IS NULL
        )
        UPDATE Baghi SET tartib = @Max + rn;
    END
END
GO

PRINT N'TAGCOD.tartib بررسی و در صورت خالی بودن پر شد.';
GO
